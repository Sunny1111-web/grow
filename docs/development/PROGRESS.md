# 开发账本

2026-09-05：用户启动长期目标“完成 grow 游戏的开发，阶段性的提交 git”。重新核查：仅 docs，无 project.godot，Git master 无提交；本机 Godot 4.7.2 工具就绪。设计文档交付完成不等于游戏完成。开始阶段0，按 IMPLEMENTATION_PLAN.md 执行。

2026-09-05 阶段1基础实现：PlantState与水/光/结构/几何求解器已建立，Godot headless导入成功，148项断言通过。实际两根接W1、三藤终点(3.692749,1.04)锚定椅座且首叶稳定普通光成立；修复了吸附后末切线穿入椅座的问题。400边/100叶冒烟约142ms（结构约119ms），尚未达到最终性能门禁。命令事务、模拟、交互、救援、存档、完整关卡与发布尚待实现。并行代理遇用量限制后由主代理继续；未获得独立代码审查。

2026-09-05 阶段2核心循环：新增命令预览/确认完整性、槽位与费用、强化、修剪与一次侧芽权益、固定救援、0.1秒模拟、局部枯萎/断裂、催生、冻结与Godot输入/HUD/初期场景。642项断言通过；真实OpenGL窗口鼠标回放首叶成功(5边1叶、0.96E/s)，无渲染错误。完整关卡、保存、历史合批、教程、美术音频、压力优化与发布仍未完成，逐项列于ACCEPTANCE.md。证据目录test-results/run-20260905-174607-932-8c3f27及run-20260905-174608-721-b5234c。

2026-09-05 阶段3通关与存档：实现不可变世代保存、坏代回退/未来格式拒绝、菜单继续、F5与事件/退出保存；一次教学、水壶5秒记忆、真实活藤3秒通关、2秒全株镜头与返回标题。修复已揭示未接入水源方向过早消失、局部光源后墙误遮、亮背景HUD对比。711断言通过；真实窗口完整鼠标事件回放28边2叶，Q20/需求18.57，通关状态重新载入成功。退出通知独立进程验证生成新代且旧代保留。下一步：关卡检查器与恢复多次/上限验证、历史合批、性能算法与缓存、完整反馈/可访问性、美术音频、独立导出和真人测试。

2026-09-05 性能门禁达成（阶段4首项）：TDD先立三项失败断言后重写。水求解器改为CSR邻接+平铺二叉堆+kind编码（4.75→1.7ms）；光求解器按光区共享射线预计算、垂距剔除、真实环境矩形快速判定（20.8→0.4ms）；结构求解器sibling链平铺+手写快排+两轴worker并行（120ms后4.8→2.9ms）；metrics层三求解worker并行并按拓扑键缓存，产能每次现算。修复commit强化仅改kind导致缓存漏检失效、教学事件id与器官编号事件类型比较错误两处既有bug。结果：稳态10Hz逻辑步p95=2.6ms（≤3ms门禁）、拓扑变化全量求解p95=4.4ms（≤8ms预览门禁）、缓存命中67µs；结构差分不变、journey回放58步/119.3模拟秒与阶段3证据逐字一致。新增tests/test_logic_performance.gd，全套7713断言通过。剩余：整帧1080p窗口实测、加载/提交/30分钟稳定采样未做；关卡检查器、多次救援、历史合批、美术音频、导出与真人测试待办。

2026-09-05 关卡检查器全套：新增scripts/core/level_validator.gd覆盖TDD第12节13项报告（ID唯一含碰撞/锚点共享豁免、光区方向、水点土壤、关键点无遮挡、0.08u假缝隙双轴、W1/W2含水层一致、首根两段接水、开局26E≤30E预算、救援18E固定路线90/60/5°真实回放、首光安全、出口防单段跨越、锚点有效、W1→W2线索链0.8步连续）。TDD先写41项断言（当前关卡全过+13类破坏用例定向失败），命令行入口tools/validate_level.gd失败退出1、编辑器入口tools/level_check_editor.gd只报告不改场景。LEVEL_CHECK 13 passed。全套7753断言通过。

2026-09-05 恢复与随机拓扑验证（test_recovery.gd，451断言）：5次连续退守后18E固定路线每次成立（器官不累计、拓扑有效、产能为正）；400边100叶满上限退守预告/提交/历史500项/再退守不累计；修剪伤痕权益经不可变世代存读后同槽再生仍为7E且只用一次；固定种子20260905的1000步随机六类命令/修剪产生373次提交、每50步拓扑校验通过且器官/能量不越界；400/100上限提示与三层嵌套子树修剪全部移除。全套8203断言通过。

2026-09-05 历史表现槽合批：新增scripts/view/history_slots.gd纯函数——超过200表现槽时最近100项保持单项清晰，更旧的按时间序空间邻近聚类（半径1.5u起、超100组扩大重跑、最终硬合并）为静态笔触，边按折线、叶按菱形标记生成世界坐标线段对；world_view以draw_multiline整批一次绘制并缓存，首次合批经game.notice_history_merged提示「旧痕迹已合并显示」，逻辑history始终完整。test_history_slots.gd 28断言（上限/空间聚类/成员守恒/线段内容/场景集成提示）。全套8230断言通过；真实窗口开局回放5边1叶0.96E/s无脚本/渲染错误。

2026-09-05 音频系统与氛围层：audio_bank.gd程序化PCM合成7种动作音（生长/叶/修剪/断裂/接水/退守/通关）与8秒无缝循环环境声，无外部音频资产；audio.gd建立Ambience/SFX总线汇入Master，线性↔分贝映射，ConfigFile音量持久化；暂停菜单三条音量滑块实时调节并保存；命令提交/事件/通关挂接播放。world_view新增常态氛围层（光区径向光晕、已揭示水源呼吸波光）。assets/LICENSES.md记录全部资产程序自制与Godot MIT引擎许可。test_audio.gd 39断言（峰值/时长/总线/音量映射/存读/事件/UI）。全套8268断言通过；窗口开局截图人工视检：中文可读、HUD不重叠、光晕与光柱正常。

2026-09-05 首次真人通关（用户本人，DEBUG运行，截图存档）：通关卡片「第一章·空房间 完成」正常弹出；通关时能量15.0/40、供水20/需求19.84、产能+1.92E/秒、风险1.00。需求距供水上限仅0.16，证明路线经济性自然收敛；真人操作路径与回放路径（椅座→桌缘→管道→出口）一致。此为开发者自测数据，不计入5名新玩家首玩样本。

2026-09-05 Windows独立包发布与脱离编辑器验证：安装4.7.2.stable导出模板至用户模板目录；export_presets.cfg（x86_64 release、嵌入PCK、文本脚本模式）；导出build/grow-0.1.0-windows/Grow.exe（104.8MB单文件）。release模板不支持--script自动回放（实测静默退出），改由用户人工完成独立包通关：截图确认窗口无DEBUG标记、通关卡片正常；机器校验user://第75代存档SHA256匹配、won=true、tick=1233（123.3模拟秒，接近回放119.3秒）、能量13.744与截图一致、rescue_count=0零退守一次通关、退出自动保存产生新代。README新增版本记录0.1.0与独立包导出说明。

2026-09-05 整帧性能采样与分层渲染（tools/capture_perf.gd，1080p窗口禁vsync、400边/100叶/220历史压力株）：初测帧p95=116ms，根因为每帧全量重绘（每叶约35个绘制调用）。重构为三层CanvasItem缓存：background_layer仅相机/揭示变化重绘（30秒内仅1次）、growth_layer与模拟同频10Hz、overlay随父每帧；validate从逐节点爬链O(n²)改为一次种子可达遍历O(n)（42.4→2.5ms，链形最坏情况）；clone按字段变更语义浅拷贝（只增数组与points/anchor共享，z/bend/angle隔离）；状态摘要缓存（revision/能量/时钟键）+摘要排除history内容（保留size）。终测：帧CPU成本p95=2.9ms（≤16.7门禁）、中位1.6ms、逻辑步p95=2.3ms（≤3ms）、提交p95=8.5ms（≤100ms）、存读p95=159ms（≤2s）均达标；预览成功路径13.7ms超8ms门禁（分段：clone1.3+build0.6+metrics3.4+validate2.7+digest候选2.3），已推至编辑器debug环境极限，release构建预期减半但未实测——用户裁定不在导出包内继续采集，该项留待后续。delta口径说明：脚本窗口失焦触发引擎节流（delta≈120ms），帧门禁按CPU端帧成本采样。全套8268断言通过。

2026-09-05 输入模式回归与可视化补全（test_input_modes.gd，21断言）：失焦冻结语义验证（FOCUS_OUT冻结经济时间、清空预览/感知/修剪/平移、FOCUS_IN恢复流动、菜单嵌套保留）；Tab重叠目标循环切换；新增剪叶预览高亮（world_view.cut_highlight数据与绘制共用，剪边预览不误报）；新增结构吃力警示（bend>0枝条暖色描边，danger_edge_ids数据源，与缺水色分离）。修复分层重构的CanvasItem绘制上下文错误：引入brush当前绘制目标，层脚本在自身_draw中切换，全部23处绘制调用重定向；完整窗口通关回放恢复通过（won=true、28边2叶、无渲染错误），通关截图人工视检正常。全套8289断言通过。

2026-09-05 发布包重导出收尾：发现此前的release包（21:31）不含分层渲染重构与brush修复后的最新代码，重新导出release（Grow.exe 104.9MB，22:38）与debug验证包（GrowDebug.exe，22:38）至最新提交b27ca1f。脱离编辑器通关验证沿用用户此前在独立包中的人工通关与第75代存档机器校验证据，最新代码等价性由8289断言全套与完整窗口回放覆盖；用户裁定不再重复脱离编辑器通关验证。
